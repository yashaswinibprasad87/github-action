
name:First Demo

on:
  workflow_dispatch:

jobs:
  build:
    runs on: ubuntu latest
    steps:
      - name: Checkout code
        user: actions/checkout@4

      - name: Build
        run: echo "BuildCode"

